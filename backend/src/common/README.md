# Common

Code transversal partage par plusieurs modules.

- `decorators`: helpers Nest reutilisables comme `CurrentUser`.
- `filters`: format uniforme des erreurs HTTP.
- `guards`: protections d'acces et roles.
- `interceptors`: format uniforme des reponses API.
