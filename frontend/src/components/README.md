# Components

Reusable presentation components live here.

- `ui/`: generic primitives such as buttons, fields, modals and toasts.
- `shared/`: reusable app components shared by features.

Components in this folder should avoid business logic.
